VERSION = 1.0
HOST = '0.0.0.0'
PORT = 1991
#Timeoutout in secondes per each HTTP REQUEST to offshore APIs that this
#server makes per each endpoint.
TIMEOUT = 1
DEBUG = True
PEDANTIC_INIT = True

HTTP_HEADERS = {
    'Accept' : 'Content-Type: application/json; charset=utf-8; version=1.0',
    'Access-Control-Allow-Origin' : '*'
}

#Allow generating Random data when can't ping offshore API.
# ALLOW_SPOOF = False

_base_url = 'http://localhost:1234/'
ENDPOINTS = {
    'base' : _base_url,
    'event_add' : 'add/subscribe',
    'systems' : 'Systems',
    'registries' : 'Registries'
}

"""
[optional] Time in seconds to wait to check for ENPOINTS' 'event_*' subscribtion
if failed to subscribe/ping endpoint on this server's start.
"""
SUBSCRIBE_INTERVAL = 2